# 新しいアプリをこのストアに足す手順

> **リポジトリが違っても問題ない。** このページは各アプリの**公開URLを並べているだけ**で、コードは持っていない。
> アプリ本体はそれぞれ独立リポジトリ＋独立のGitHub Pagesで動く（例：`mimi-lite-pwa`）。

## 足すのは3か所だけ

1. **アイコン**：`<次の番号>.png` をリポジトリ直下に置く（アプリ側の `icons/icon-512.png` をそのままコピーでよい）
2. **カード**：`index.html` の `<div class="apps-grid">` の最後に1ブロック追加

```html
<div class="app-card" onclick="openModal('＜キー＞')">
    <div class="app-icon">
        <img src="＜番号＞.png" alt="＜表示名＞">
    </div>
    <div class="app-name">＜表示名＞</div>
    <div class="app-category">＜カテゴリ＞</div>
</div>
```

3. **データ**：`const apps = {` の直後に1エントリ追加（先頭に足すと末尾カンマの事故が起きない）

```js
＜キー＞: {
    name: '＜表示名＞',
    category: '＜カテゴリ＞',
    icon: '＜番号＞.png',
    description: '＜1〜2文。何を測る/何をする道具か＞',
    features: ['＜箇条書き5つまで＞'],
    url: 'https://hokutomiyazaki-arch.github.io/＜リポジトリ名＞/'
},
```

## 足したあとの確認（2つだけ）

- **カードの数と `apps` のキーの数が一致しているか**（カードだけ足してデータを忘れると、押しても何も出ない）
- **アイコンが実際に表示されるか**（ファイル名の番号ずれが一番多い）

## ブランドガード

`description` と `features` にも適用する。「治る」「消える」は書かない。宮崎の肩書に「治療家」は使わない。「神経学」ではなく「神経科学」。

## 収録の基準（2026-08-20 時点の実態）

- ここは**パスワードで守られたプロ向けの棚**。一般視聴者向けのLINE配布とは別物
- **LINE配布しているLite PWA（こきゅう／ろうがん／しや）は、まだここに載っていない**。載せるかどうかは宮崎の判断
