# ★このページの位置づけ（2026-08-20 宮崎確定・絶対）

**このページはコミュニティ会員だけの特典。** 入るには**会員だけが知っているパスワード**が要る。

- **ページを共有する導線を作らない。** 「このページを共有」ボタン、URLのコピー、SNSシェア、OGP画像、
  QRコード、いずれも**付けない**（2026-08-20に僕が「このページを共有」ボタンを付けて宮崎に止められた）
- **パスワードを、ページ上・概要欄・LINE・READMEなど、どこにも書かない**
- 配ってよいのは**公式LINE**だけ。**公式LINE＝`@583penki`（`https://lin.ee/pXqata2`）＝Functional Neuro Trainingの公式LINE**。
  動画の概要欄では「BDCオンラインスクール公式LINE」という**表記**で出しているが**アカウントは同一**。別に「FNTのLINE」は無い
  （2026-08-20：僕がこれを別アカウントだと誤読し、正しく載っていたQRを取り下げてしまった）
- **「公式LINEで◯◯と送ってください」と書くなら、その公式LINEのQRと友だち追加リンクを必ず同じページに載せる。**
  どのLINEか分からなければ受講者もクライアントも渡せない（2026-08-20の指摘）
- `robots.txt` / `noindex` の類を足すかは宮崎判断（今は入っていない）

- **配布ワードは、LINE側に自動応答を設定してから「稼働中」として出す。** まだ設定していないワードは
  `apps` のエントリに `wordStatus: 'pending'` と `wordNote` を付ける（カードのバッジが「準備中」になり、
  モーダルの渡し方が警告表示に変わる）。設定できたらその2行を消す。
  ※2026-08-20：`みみ` を稼働中として出したが、LINEのキーワード応答が未設定で**送っても何も返らない**状態だった。
  registry（`karada-kikaku/.claude/config/deliverables-registry.md`）の status が `live` かどうかを見てから出す。

---

# 新しいアプリをこのストアに足す手順

> **リポジトリが違っても問題ない。** このページは各アプリの**公開URLを並べているだけ**で、コードは持っていない。
> アプリ本体はそれぞれ独立リポジトリ＋独立のGitHub Pagesで動く（例：`mimi-lite-pwa`）。

## 足すのは3か所だけ

1. **アイコン**：`<次の番号>.png` をリポジトリ直下に置く（アプリ側の `icons/icon-512.png` をそのままコピーでよい）
2. **カード**：`index.html` の `<div class="apps-grid">` の最後に1ブロック追加

```html
<div class="app-card" onclick="openModal('＜キー＞')">
    <div class="app-icon">
        <img src="＜番号＞.png" alt="＜表示名＞">
    </div>
    <div class="app-name">＜表示名＞</div>
    <div class="app-category">＜カテゴリ＞</div>
</div>
```

3. **データ**：`const apps = {` の直後に1エントリ追加（先頭に足すと末尾カンマの事故が起きない）

```js
＜キー＞: {
    name: '＜表示名＞',
    category: '＜カテゴリ＞',
    icon: '＜番号＞.png',
    description: '＜1〜2文。何を測る/何をする道具か＞',
    features: ['＜箇条書き5つまで＞'],
    url: 'https://hokutomiyazaki-arch.github.io/＜リポジトリ名＞/'
},
```

## 足したあとの確認（2つだけ）

- **カードの数と `apps` のキーの数が一致しているか**（カードだけ足してデータを忘れると、押しても何も出ない）
- **アイコンが実際に表示されるか**（ファイル名の番号ずれが一番多い）

## ブランドガード

`description` と `features` にも適用する。「治る」「消える」は書かない。宮崎の肩書に「治療家」は使わない。「神経学」ではなく「神経科学」。

## どちらの枠に入れるか（2026-08-20 宮崎確定）

ページは**2つの枠**に分かれている。足すときはどちらかを選ぶ。

| 枠 | 中身 | カードの書き方 |
|---|---|---|
| **評価・トレーニングアプリ** | 現場で使う評価・訓練ツール | ふつうにカテゴリだけ |
| **YouTubeで配っているアプリ** | 動画の視聴者に公式LINEのキーワードで配っているLite PWA | カテゴリの後ろに `<span class="app-word">LINE「◯◯」</span>` を付ける。`description` の末尾にも `【配布ワード】公式LINEで「◯◯」と送ると届きます。` を書く |

**YouTube枠に入れる条件**＝`karada-kikaku` の `.claude/config/deliverables-registry.md` に配布ワードが登録されていること。登録が無いものは評価・トレーニング枠へ。

## 収録状況（2026-08-20）

全21本。**評価・トレーニング17／YouTube配布4**（みみ・こきゅう・ろうがん・しや）。

## ★収録しないもの（2026-08-20 宮崎確定）

**公開されているアプリを片っ端から載せない。** ここに足すのは次の2つだけ。

1. **もともと載っている評価・トレーニングアプリ**（現場で使うもの）
2. **YouTubeで配っているアプリ**（`karada-kikaku` の `deliverables-registry.md` に配布ワードがあるもの）

新しくアプリを公開したときに自動で足すのは **2 のみ**。1 に足すかどうかは宮崎に確認する。

※過去の失敗：2026-08-20に僕（Claude）が「他のアプリも全部」を「公開中のリポジトリを全部」と読み違え、FNTと無関係なもの（子供向けゲーム等）まで14本追加してしまい、宮崎の指摘で差し戻した。**"公開されている" は "載せてよい" を意味しない。**

---

## バナー画像（YouTube配布アプリのみ）

`banners/<キー>.svg` を置くと、モーダルの先頭に表示される。**`word`（配布ワード）を持つアプリだけ**表示する実装なので、評価・トレーニング枠のアプリにバナーを置いても出ない。

- サイズ＝1200×420（`aspect-ratio` で可変）
- 中身＝カテゴリ／アプリ名／LINE配布ワードのバッジ／Functional Neuro Training
- 配色＝ブランドの紫 `#4A3A8C` → シアン `#1BA3CC`

## クライアントへの渡し方（モーダルに自動表示）

`word` を持つアプリは、説明の下にこの案内が出る：

> **クライアントへの渡し方**
> 「公式LINEで「◯◯」と送ってください」と伝えるだけで渡せます。URLを教える必要はありません。
